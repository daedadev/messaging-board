import React from "react";
import Form from "../components/LoginSignup/Form";

export default function LoginPage() {
  return (
    <section className="flex w-full min-h-screen items-center justify-center bg-slate-200">
      <Form />
    </section>
  );
}
